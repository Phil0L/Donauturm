<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Donauturm" >
        <activity
            android:name=".controller.DonauturmActivity"
            android:exported="true"
            android:label="@string/title_activity_donauturm"
            android:resizeableActivity="true"
            android:theme="@style/Theme.Donauturm.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".drinksmenu.controller.drinkmenu.drinksedit.DrinksMenuEditorActivity" />
    </application>

</manifest>