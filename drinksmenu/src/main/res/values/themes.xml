<resources>
    <!-- Base application theme. -->
    <style name="Theme.Donauturm.DrinksMenu.Editor" parent="Theme.MaterialComponents.Light">
        <item name="colorPrimary">@color/teal_700</item>
        <item name="colorPrimaryVariant">@color/teal_900</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pink_600</item>
        <item name="colorSecondaryVariant">@color/pink_400</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/black</item>
        <item name="strokeColor">@color/gray_500</item>

        <!-- Action bar color. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Status bar color. -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/teal_50</item>
        <item name="statusBarBackground">@color/teal_50</item>
    </style>

    <style name="Theme.Donauturm.DrinksMenu.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Donauturm.DrinksMenu.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>